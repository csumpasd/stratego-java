public class Main {
    public static void main(String[] args) {
        /* init
         * Emperor Rose places all pieces
         * whichplayersturn invert through "other player" button
         * Emperor Blau places all pieces
         *
         * go into next phase
         */

        /* repeat until game ends
         * whichplayersturn invert through "other player" button, only show current player's pieces, (rotate board?)
         * current player selects piece
         * selects move to take, takes it if empty
         * if stepped on another piece, calls it's steppedOn, then the piee that was stepped on figures out what to do and tells the other one too
         * either moves back, dies, or moves there
         */

        //outit
    }
}